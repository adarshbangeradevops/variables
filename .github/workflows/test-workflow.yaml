name: GitHub Actions Demo
run-name: Github Action env demo🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: List files in the repository
        run: printenv
      - name: Operating System it is running 
        run: echo " The Operation system the job is running  ${{ runner.os }}"
      - name: Branch on which the pipeline is running
        run: echo " The branch on which the job is running ${{ github.ref }}"
